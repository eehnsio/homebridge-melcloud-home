{
  "pluginAlias": "MELCloudHome",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "MELCloud Home"
      },
      "refreshToken": {
        "title": "Refresh Token",
        "type": "string",
        "description": "OAuth refresh token obtained via the Settings button above. Click Settings → Login via Browser to authenticate.",
        "placeholder": "Click the Settings button above to authenticate"
      },
      "refreshInterval": {
        "title": "Device Status Refresh Interval (seconds)",
        "type": "integer",
        "default": 90,
        "minimum": 30,
        "maximum": 600,
        "description": "How often to poll MELCloud API for device status updates. Recommended: 60-120 seconds. This detects changes made via MELCloud app or physical remote. Note: Changes made via HomeKit update instantly."
      },
      "debug": {
        "title": "Debug Mode",
        "type": "boolean",
        "default": false,
        "description": "Enable debug logging"
      }
    }
  }
}
